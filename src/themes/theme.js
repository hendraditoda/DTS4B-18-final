import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'white',
    primary: {
      main: '#FDFDFD',
    },
    secondary: {
      main: '#444444',
    },
  },
});

export default theme;
